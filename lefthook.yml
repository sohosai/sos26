pre-commit:
  parallel: true
  commands:
    biome:
      glob: "**/*.{js,ts,jsx,tsx,json,jsonc,cjs,mjs,cts,mts}"
      run: bunx biome check --write {staged_files}
      stage_fixed: true
    prisma:
      glob: "prisma/schema.prisma"
      run: bun run db:format
      stage_fixed: true
